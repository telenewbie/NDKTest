<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".DnsActivity"></activity>
        <activity android:name=".CharsetUtilsActivity" />
        <activity android:name=".CoroutinesActivity" />
        <activity android:name=".PIDActivity" />
        <activity android:name=".LogActivity" />
        <activity android:name=".ButterKnifeActivity" />
        <activity android:name=".AnnotationActivity" />
        <activity android:name=".HookActivity" />
        <activity android:name=".StackActivity" />
        <activity android:name=".ThreadActivity" />
        <activity android:name=".ProxyActivity" />
        <activity android:name=".TestCXXInvokeActivity" />
        <activity android:name=".MemoryActivity" />
        <activity android:name=".RenameThreadActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>